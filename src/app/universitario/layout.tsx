import AppShell from '@/components/layout/AppShell'

export default function UniversitarioLayout({ children }: { children: React.ReactNode }) {
  return <AppShell colorScheme="dark">{children}</AppShell>
}
